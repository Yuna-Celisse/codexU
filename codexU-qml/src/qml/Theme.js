.pragma library

// ── Window ──
var windowBg      = Qt.rgba(185/255, 218/255, 230/255, 0.985);
var windowBorder  = Qt.rgba(1, 1, 1, 0.42);
var windowRadius  = 24;

// ── Section / card surfaces ──
var sectionFill   = Qt.rgba(235/255, 248/255, 252/255, 0.88);
var sectionBorder = Qt.rgba(1, 1, 1, 0.45);
var cardFill      = Qt.rgba(250/255, 253/255, 255/255, 0.95);
var cardBorder    = Qt.rgba(1, 1, 1, 0.50);

// ── Task column accent tints (lighter, more subtle) ──
var taskActiveBg    = Qt.rgba(245/255, 158/255, 11/255, 0.12);
var taskPendingBg   = Qt.rgba(148/255, 163/255, 184/255, 0.14);
var taskScheduledBg = Qt.rgba(139/255, 92/255, 246/255, 0.13);
var taskDoneBg      = Qt.rgba(34/255, 197/255, 94/255, 0.13);

// ── Task card ──
var taskCardBg      = Qt.rgba(1, 1, 1, 0.90);
var taskCardBorder  = Qt.rgba(1, 1, 1, 0.50);

// ── Brand ──
var brandBlue    = "#3b82f6";
var brandPurple  = "#8b5cf6";
var brandTeal    = "#14b8a6";
var brandOrange  = "#f59e0b";
var brandGreen   = "#22c55e";

// ── Text ──
var textPrimary   = "#0f172a";
var textSecondary = "#475569";
var textMuted     = "#64748b";
var textWeak      = "#94a3b8";

// ── Progress bar track ──
var trackFill = Qt.rgba(148/255, 163/255, 184/255, 0.25);

// ── Radii ──
var radiusCard    = 18;
var radiusSmall   = 14;
var radiusTask    = 12;
var radiusButton  = 12;

// ── Font sizes (pixels) ──
var fsTitle    = 28;
var fsValue    = 24;
var fsSub      = 14;
var fsBody     = 13;
var fsSmall    = 11;
var fsTiny     = 10;
